specVersion: "v1beta"
serviceId: "webappfinal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webappfinal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
